import 'package:flutter/material.dart';

class ResigterFormField extends StatelessWidget {
  const ResigterFormField({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
